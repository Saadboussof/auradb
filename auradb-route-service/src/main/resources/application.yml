server:
  port: 8082

spring:
  application:
    name: auradb-route-service
  data:
    redis:
      # In Docker, it connects to 'auradb-redis'. If running locally outside Docker, it falls back to 'localhost'
      host: ${REDIS_HOST:localhost}
      port: 6379
